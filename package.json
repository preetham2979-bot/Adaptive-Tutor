{
  "name": "adaptive-tutor",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Adaptive Tutor — Knowledge Tracing Agent. BKT engine decides what's next; Llama 3.3 70B (via Groq) generates the question/hint/explanation text.",
  "scripts": {
    "dev": "node --watch server/index.js",
    "start": "node server/index.js",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "engines": {
    "node": ">=22.5.0"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "cookie-parser": "^1.4.6",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "groq-sdk": "^1.3.0",
    "jsonwebtoken": "^9.0.2",
    "openai": "^6.48.0"
  },
  "devDependencies": {
    "vitest": "^1.6.0"
  }
}
